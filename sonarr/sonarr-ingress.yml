apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sonarr
  # annotations:
    # kubernetes.io/tls-acme: "true"
spec:
  rules:
  - host: sonarr.momochan.com
    http:
      paths:
      - path: /
        backend:
          service:
            name: sonarr
            port:
              number: 80
        pathType: Prefix
